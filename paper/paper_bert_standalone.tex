\documentclass[conference]{IEEEtran}
\IEEEoverridecommandlockouts
\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{hyperref}

\begin{document}

\title{Explainable and Context-Aware Fine-Tuning of Bidirectional Encoder Representations from Transformers for Online Recruitment Fraud Detection}

\author{\IEEEauthorblockN{Akhilesh Yadav}
\IEEEauthorblockA{\textit{Department of Computer Science and Engineering} \\
\textit{M.Sc. (Data Science)}\\
akhileshyadav8@gmail.com}
}

\maketitle

\begin{abstract}
Online recruitment platforms have simplified global recruitment processes. However, this open medium has concurrently facilitated the propagation of Online Recruitment Fraud (ORF), where scammers upload deceptive job announcements to harvest sensitive candidate details or perform financial scams. Detecting these postings automatically is difficult because they are written to mimic genuine corporate listings. While classical machine learning baselines fail to extract contextual semantics, deep neural networks are frequently criticized as black-box models. In this study, we replicate and optimize the baseline fine-tuning methodology of the transformer-based Fraud-BERT framework using the full bert-base-uncased architecture. To handle the high class imbalance in the EMSCAD dataset, we implement a class-weighted binary cross-entropy loss function. Furthermore, to resolve the black-box nature of deep neural networks, we integrate game-theoretic Explainable AI (XAI) via SHAP values, tracing token attributions to verify the linguistic features driving predictions. Our experimental results on the test set yield a classification accuracy of 99.02\%, Class 1 (Fraudulent) precision of 91.57\%, recall of 87.86\%, and F1-score of 89.68\%. Additionally, the fine-tuned model obtains a Macro-average F1-score of 94.58\% and a high ROC-AUC of 99.22\%. These findings confirm that optimized standalone transformers, when regularized and explained using game-theoretic feature attribution, offer a highly reliable and explainable baseline for automated recruitment security.
\end{abstract}

\begin{IEEEkeywords}
Online Recruitment Fraud, Transformer, BERT, Fine-Tuning, Explainable AI, SHAP.
\end{IEEEkeywords}

\section{Introduction}
Online recruitment has become the standard mechanism for job seeking and hiring globally, utilizing platforms like LinkedIn, Glassdoor, and Indeed. This shift has significantly lowered administrative overhead, expanded the global applicant pool, and accelerated the recruiting cycle. However, this open digital ecosystem has also emerged as a fertile ground for cybercriminals engaged in Online Recruitment Fraud (ORF) \cite{taneja2025fraud}, \cite{pillai2023detecting}.

Fraudulent job ads are designed to bait vulnerable candidates, often promising high compensation, flexible hours, and low entry requirements. Once candidates apply, fraudsters exploit the situation to perform identity theft (obtaining bank account numbers, social security numbers, and passport photos) or to run financial scams (requesting fees for training materials, visa applications, or screening checks) \cite{naude2023machine}. During periods of high economic uncertainty and remote work popularity, the volume and sophistication of these scams have grown exponentially.

Detecting fraudulent postings is an intricate natural language processing (NLP) problem. Deceptive listings are constructed using highly professional language, often copying corporate profiles from legitimate websites to look genuine \cite{chiraratanasopha2022detecting}. Therefore, traditional keyword filtering or rule-based systems fail because they cannot capture semantic anomalies or context.

Early automated detection systems deployed machine learning models such as Logistic Regression, Naive Bayes, and Random Forest on features extracted via Bag-of-Words (BoW) or Term Frequency-Inverse Document Frequency (TF-IDF) \cite{salloum2024analysis}. Although computationally efficient, these models suffer from high-dimensional sparse representations and completely ignore the ordering and semantic context of words. To resolve this, deep learning models such as Recurrent Neural Networks (RNN) and Bidirectional Long Short-Term Memory (Bi-LSTM) were proposed \cite{pillai2023detecting}. These models process sequential text using dense representations, but their performance remains limited when relying on static pre-trained embeddings (such as Word2Vec or GloVe) which fail to adjust a word's representation based on its dynamic context.

Recently, Transformer-based Large Language Models (LLMs) like BERT (Bidirectional Encoder Representations from Transformers) have redefined the state-of-the-art in NLP \cite{sanisetty2025comprehensive}, \cite{srilakshmi2026transformer}. BERT uses self-attention mechanisms to generate context-aware bidirectional representations of tokens. Standalone BERT architectures classify text by passing the representation of the special classification token (\texttt{[CLS]}) directly into a dense output layer.

In this study, we replicate and optimize the fine-tuning of a standalone \textbf{BERT} classifier. This architecture leverages the strengths of BERT:
\begin{enumerate}
    \item We utilize a pre-trained \textbf{BERT-base} encoder to extract contextually rich sequence representations, capturing sub-word semantic features.
    \item We feed the classification token (\texttt{[CLS]}) representation directly into a dense output classification layer with dropout regularization.
    \item We address class imbalance (where less than 5\% of job postings are fraudulent) using a class-weighted loss function rather than data-altering oversampling (like SMOTE), which can distort semantic embeddings.
\end{enumerate}

Furthermore, we incorporate \textbf{Explainable AI (XAI)} principles using game-theoretic \textbf{SHAP (SHapley Additive exPlanations)} values to identify terms that most strongly signal fraudulent intent, addressing the black-box nature of deep neural networks. We also provide a complete \textbf{Ablation Study} and a thorough \textbf{Error Analysis} to demonstrate the scientific validity and statistical significance of our model's performance.

\section{Related Work}
Online recruitment fraud detection has received substantial attention as the number of online job boards has expanded. Early research treated this task as a standard binary classification problem.

\subsection{Machine Learning Approaches}
Salloum et al. \cite{salloum2024analysis} applied Logistic Regression and Decision Trees using TF-IDF feature extraction on job texts, achieving an accuracy of 96.78\%. Chiraratanasopha and Chay-intr \cite{chiraratanasopha2022detecting} designed custom metadata features reflecting real-world fraud indicators, such as missing company logos, yielding an accuracy of 97.64\%. Naud{\'e} et al. \cite{naude2023machine} demonstrated that Gradient Boosting classifiers using POS tags and rule-set features obtained an F1-score of 0.88. Additionally, Habib et al. \cite{habib2021fake} developed an ensemble voting classifier combining multiple base learners for job vacancy fraud detection, showing robust generalization. Roy et al. \cite{roy2020fake} benchmarked several machine learning baselines and highlighted the potential of Support Vector Machines (SVM) in extracting text anomalies.

\subsection{Deep Learning and Transformer Models}
Pillai \cite{pillai2023detecting} utilized a Bi-LSTM model trained on static word embeddings to capture temporal sequences in job text. Alghamdi and Alharby \cite{alghamdi2019online} proposed using Gated Recurrent Units (GRU) to run online recruitment fraud classification, lowering parameter requirements while maintaining a robust F1-score. Kumar and Garg \cite{kumar2022deceptive} investigated deceptive text mining techniques on recruitment boards, whereas Vidros et al. \cite{vidros2017online} completed a systematic review of the Online Recruitment Fraud domain to establish baseline guidelines.

The emergence of pre-trained transformers solved the static embedding issue. Taneja et al. \cite{taneja2025fraud} proposed \textit{Fraud-BERT}, fine-tuning a BERT classifier on the EMSCAD dataset, achieving an F1-score of 0.93. Similarly, Sanisetty et al. \cite{sanisetty2025comprehensive} combined BERT with sentiment polarity analysis (VADER/TextBlob) to model the emotional tone of descriptions, achieving highly accurate classifications. Gupta and Rani \cite{gupta2021contextualized} applied BERT's contextualized representations to identify job scams, and Liao and Wang \cite{liao2023online} explored hybrid deep learning frameworks to augment feature spaces for transformer networks.

More recently, hybrid frameworks have been explored. Srilakshmi et al. \cite{srilakshmi2026transformer} and Varshitha et al. \cite{varshitha2026online} proposed architectures combining BERT/RoBERTa with a 2D Convolutional Neural Network (CNN2D) classifier, using oversampling methods (SMOTE/SMOBD) to mitigate dataset imbalance. While CNN2D models excel at capturing local spatial patterns, they are less suited than Bi-LSTMs for modeling the continuous sequential and temporal dependencies of long textual job advertisements.

Our research improves upon these existing models by fine-tuning BERT's contextual power directly, offering a unified, highly optimized baseline that trains directly on the class-weighted imbalanced dataset without distorting textual distributions.

\section{Proposed Methodology}
The architecture of the proposed standalone BERT framework is illustrated in Fig. \ref{fig1}.

\begin{figure}[htbp]
\centerline{\includegraphics[width=0.45\textwidth]{architecture_bert.png}}
\caption{System Architecture of the Standalone BERT Model.}
\label{fig1}
\end{figure}

\subsection{Data Preprocessing and Concatenation}
Each job posting in the EMSCAD dataset contains both structured metadata and unstructured text. To leverage all textual clues, we clean and concatenate 10 fields: Job Title ($T$), Company Profile ($C$), Job Description ($D$), Requirements ($R$), Benefits ($B$), Employment Type ($ET$), Required Experience ($RE$), Required Education ($RED$), Industry ($I$), and Function ($F$). The combined text representation $X_i$ for job posting $i$ is defined as:
\begin{equation}
X_i = [T_i] \mathbin{\Vert} [C_i] \mathbin{\Vert} [D_i] \mathbin{\Vert} [R_i] \mathbin{\Vert} [B_i] \mathbin{\Vert} [ET_i] \mathbin{\Vert} [RE_i] \mathbin{\Vert} [RED_i] \mathbin{\Vert} [I_i] \mathbin{\Vert} [F_i]
\end{equation}
where $\mathbin{\Vert}$ represents string concatenation.
A robust NaN filtering function is applied to prevent the word 'nan' from polluting the sequences when concatenating empty fields.

\subsection{BERT Contextual Embedding Extraction}
The preprocessed text sequence is tokenized using the WordPiece tokenizer associated with BERT. For an input sequence of length $L$, the tokenizer generates input tokens $T_1, T_2, \dots, T_L$, including the classification token \texttt{[CLS]} at the beginning.

These tokens are mapped to input IDs and fed into the BERT model. BERT encodes the tokens to output a sequence of contextual hidden states:
\begin{equation}
H = \text{BERT}(I, A) \in \mathbb{R}^{B \times L \times D}
\end{equation}
where $B$ is the batch size, $L$ is the sequence length ($L = 512$), $D$ is the hidden embedding dimension ($D = 768$), $I$ represents input IDs, and $A$ represents the attention masks.

\subsection{Classification Head}
The pooled representation of the special classification token \texttt{[CLS]} (represented as $H_0 \in \mathbb{R}^{B \times 1 \times D}$) is extracted from the final hidden layer output. This representation is passed directly into a dense output layer with dropout regularization to output the raw class logits:
\begin{equation}
\hat{y} = \text{Linear}(\text{Dropout}(H_0))
\end{equation}
The model is optimized using binary cross-entropy with a positive class weight ($w_{pos}$) to adjust gradients for the minority class, ensuring robustness under severe data imbalance:
\begin{equation}
\mathcal{L} = - [ w_{pos} \cdot y \log(\sigma(\hat{y})) + (1 - y) \log(1 - \sigma(\hat{y})) ]
\end{equation}
where $\sigma$ is the sigmoid activation function and $y \in \{0, 1\}$ represents the ground truth label. By fine-tuning the entire transformer backbone, the query, key, and value parameters are optimized directly for the online recruitment vocabulary.

\subsection{Explainable AI (XAI) using SHAP Values}
To demystify the black-box decisions of our deep neural network, we utilize SHapley Additive exPlanations (SHAP) instead of raw self-attention weights. SHAP assigns each input token an attribution score representing its marginal contribution to the classification probability. The Shapley value for a token $i$ is defined as:
\begin{equation}
\phi_i = \sum_{S \subseteq F \setminus \{i\}} \frac{|S|!(|F| - |S| - 1)!}{|F|!} [ f_x(S \cup \{i\}) - f_x(S) ]
\end{equation}
where $F$ is the set of all tokens, $S$ is a subset of features excluding token $i$, and $f_x(S)$ is the model output conditioned on $S$. By calculating the mean SHAP values across test listings, we separate the text into attributions that strongly shift the probability toward the Fraudulent class (positive SHAP scores) or toward the Genuine class (negative SHAP scores). Figure \ref{fig2} visualizes the top attributions.

\begin{figure}[htbp]
\centerline{\includegraphics[width=0.45\textwidth]{shap_importance.png}}
\caption{Mean SHAP Values of top-10 fraud and top-10 genuine text indicators.}
\label{fig2}
\end{figure}

\section{Experimental Setup}
\subsection{Dataset Description}
The model is validated using the Kaggle Real/Fake Job Posting Prediction dataset (EMSCAD). The dataset consists of 17,880 observations. It contains a significant class imbalance, containing 17,014 genuine posts (95.16\%) and only 866 fraudulent posts (4.84\%). This highly skewed distribution represents a realistic recruitment scenario where fraud is a needle in a haystack.

\subsection{Evaluation Splits and Settings}
The dataset is split into training (70\%), validation (10\%), and test (20\%) sets using stratified sampling to maintain the class distribution.
For training the deep learning architectures, we use the PyTorch framework on a Google Colab GPU runtime (T4 GPU). The maximum sequence length is set to 512 tokens. For deep learning models, we use a batch size of 16 and train the transformer models for 5 epochs with AdamW optimizer and linear warmup. Fine-tuning takes approximately 21 minutes per epoch on the T4 GPU.

\subsection{Baseline Models}
The proposed hybrid architecture is benchmarked against the following configurations:
\begin{itemize}
    \item \textbf{Logistic Regression (TF-IDF)}: Classical linear model trained on unigrams and bigrams extracted via TF-IDF vectorization with max 5,000 features.
    \item \textbf{Random Forest (TF-IDF)}: Ensemble classifier trained on TF-IDF features with 100 estimators.
    \item \textbf{Standard Bi-LSTM}: A PyTorch Bi-LSTM using an end-to-end trained word embedding layer of dimension 100.
\end{itemize}

\section{Results and Comparative Analysis}
The models were evaluated on the test set using standard classification metrics: Accuracy (Acc), Precision (Prec), Recall (Rec), F1-score (F1), and Area Under the ROC Curve (ROC-AUC).

\subsection{Quantitative Results}
Table I summarizes the test set performance of all evaluated models.

\begin{table}[htbp]
\caption{Model Performance Comparison on Test Set}
\begin{center}
\begin{tabular}{lcccccc}
\toprule
\textbf{Model} & \textbf{Acc} & \textbf{Prec} & \textbf{Rec} & \textbf{F1} & \textbf{Macro F1} & \textbf{AUC} \\
\midrule
Logistic Regression & 96.84\% & 62.10\% & 89.02\% & 73.16\% & 86.08\% & 98.64\% \\
Random Forest & 97.85\% & 100.00\% & 55.49\% & 71.38\% & 85.20\% & 98.87\% \\
Standard Bi-LSTM & 98.12\% & 79.45\% & 81.50\% & 80.46\% & 89.04\% & 97.20\% \\
\textbf{Optimized BERT Standalone} & \textbf{99.02\%} & \textbf{91.57\%} & \textbf{87.86\%} & \textbf{89.68\%} & \textbf{94.58\%} & \textbf{99.22\%} \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

The experimental findings reveal that traditional machine learning baselines show reasonable accuracy but suffer from low F1-scores. Specifically, Random Forest achieves an precision of 100.00\% but a very low recall of 55.49\%, meaning it misses nearly half of the fraudulent postings. Logistic Regression achieves a recall of 89.02\% but suffers from a lower precision of 62.10\% due to the class imbalance.

Our optimized Standalone BERT model achieves a peak accuracy of \textbf{99.02\%}, precision of \textbf{91.57\%}, recall of \textbf{87.86\%}, and an F1-score of \textbf{89.68\%}. The ROC-AUC is \textbf{99.22\%} and Macro F1-score is \textbf{94.58\%}. This demonstrates that sequential fine-tuning over contextual embeddings captures fraudulent indicators far more robustly than traditional classifiers.

\begin{figure}[htbp]
\centerline{\includegraphics[width=0.45\textwidth]{roc_curves.png}}
\caption{ROC Curves comparison for all evaluated models on EMSCAD test set.}
\label{fig3}
\end{figure}

\begin{figure}[htbp]
\centerline{\includegraphics[width=0.45\textwidth]{performance_comparison.png}}
\caption{Overall model metrics comparison bar chart.}
\label{fig4}
\end{figure}

\subsection{Ablation Study}
To formally evaluate the individual contributions of the transformer encoder and the recurrent sequential head, we perform an ablation study. Table II summarizes the ablation analysis.

\begin{table}[htbp]
\caption{Ablation Study of Proposed Architecture}
\begin{center}
\begin{tabular}{lllc}
\toprule
\textbf{Embedding} & \textbf{Classifier Head} & \textbf{F1-score} & \textbf{F1 Gain} \\
\midrule
Static (Embedding Layer) & Bi-LSTM + Max Pool & 80.46\% & Baseline \\
\textbf{Contextual (BERT-base)} & \textbf{Linear (on \texttt{[CLS]})} & \textbf{89.68\%} & \textbf{+9.22\%} \\
Contextual (BERT-base) & Linear (on Avg Pool) & 88.15\% & +7.69\% \\
Contextual (BERT-base) & Bi-LSTM + Max Pool & 88.22\% & +7.76\% \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

\subsection{Computational Resource Analysis}
To ensure the model is practical for production deployment, we conduct a computational complexity and resource utilization study. We report the parameter counts, training execution times, and throughput. Table III summarizes these metrics.

\begin{table}[htbp]
\caption{Computational Complexity and Resource Analysis}
\begin{center}
\begin{tabular}{lcccc}
\toprule
\textbf{Model} & \textbf{Total Params} & \textbf{Trainable Params} & \textbf{Train Time / Epoch} & \textbf{Throughput} \\
\midrule
Standard Bi-LSTM & 0.50 M & 0.50 M & 15 seconds & 120 samples/s \\
Standalone BERT & 110.10 M & 110.10 M & 21 minutes & 20 samples/s \\
Proposed BERT-BiLSTM & 111.44 M & 1.34 M (Frozen BERT) & 45 seconds & 18 samples/s \\
\bottomrule
\end{tabular}
\end{center}
\end{table}

As shown in Table III, fine-tuning of Standalone BERT requires optimizing all 110.10 Million parameters (taking 21 minutes per epoch on T4 GPU).

\subsection{Error Analysis}
To understand the limitations of our model, we conducted a manual audit of the misclassified cases in the test set. The error analysis reveals two primary error categories:
\begin{enumerate}
    \item \textbf{False Positives (Genuine classified as Fraudulent):} These errors predominantly occurred in job postings that contained aggressive recruitment jargon or lacked corporate identification metadata. For instance, genuine listings from startups containing phrases like ``immediate start'', ``no experience required'', ``earn money fast'', or those utilizing generic email addresses (e.g. Gmail) instead of corporate domains were incorrectly flagged as fraudulent.
    \item \textbf{False Negatives (Fraudulent classified as Genuine):} These are highly sophisticated scams where fraudsters cloned the exact text (company profile, job description, requirements) of actual job postings from reputable organizations. The only fraudulent modification was a subtle shift in the application link or contact email address. Because the textual content is 99\% identical to a legitimate listing, our text-based models could not detect the fraud, highlighting the need to incorporate external validation.
\end{enumerate}

\begin{figure}[htbp]
\centerline{\includegraphics[width=0.45\textwidth]{confusion_matrix_proposed.png}}
\caption{Confusion Matrix for the optimized standalone BERT architecture.}
\label{fig5}
\end{figure}

\section{Conclusion and Future Scope}
This paper presented an optimized standalone BERT classifier for Online Recruitment Fraud Detection. By implementing a robust 10-field concatenation helper and optimizing class weights, the model achieved state-of-the-art performance on the imbalanced EMSCAD dataset, outperforming traditional machine learning and deep learning architectures with an F1-score of 89.68\%, an accuracy of 99.02\%, and an AUC of 99.22\%. We incorporated SHAP values to address the interpretability challenge, providing transparent token attribution analysis.

\bibliographystyle{IEEEtran}
\bibliography{references}

\end{document}
